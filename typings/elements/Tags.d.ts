export interface TagsAttributes {
    attached: boolean;
}
export interface TagsComputed {
    classes: string;
}
export declare const Tags: import("expangine-ui").Component<TagsAttributes, never, never, never, TagsComputed>;
