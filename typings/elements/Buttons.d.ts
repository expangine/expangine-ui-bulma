export interface ButtonsAttributes {
    attached: boolean;
    alignment: boolean;
}
export interface ButtonsComputed {
    classes: string;
}
export declare const Buttons: import("expangine-ui").Component<ButtonsAttributes, never, never, never, ButtonsComputed>;
